import React from 'react';
import { storiesOf } from '@storybook/react';
import WorkerForm from './WorkerForm';

storiesOf('2. Molecules / WorkerForm', module)
	.add('Default', () => <WorkerForm />);
