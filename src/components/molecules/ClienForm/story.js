import React from 'react';
import { storiesOf } from '@storybook/react';
import ClientForm from './ClientForm';

storiesOf('2. Molecules / ClientForm', module)
	.add('Default', () => <ClientForm />);
