import React from 'react';
import { storiesOf } from '@storybook/react';
import AccountantForm from './AccountantForm';

storiesOf('2. Molecules / AccountantForm', module)
	.add('Default', () => <AccountantForm />);
