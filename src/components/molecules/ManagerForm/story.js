import React from 'react';
import { storiesOf } from '@storybook/react';
import ManagerForm from './ManagerForm';

storiesOf('2. Molecules / ManagerForm', module)
	.add('Default', () => <ManagerForm />);
