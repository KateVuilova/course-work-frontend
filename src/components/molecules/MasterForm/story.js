import React from 'react';
import { storiesOf } from '@storybook/react';
import MasterForm from './MasterForm';

storiesOf('2. Molecules / MasterForm', module)
	.add('Default', () => <MasterForm />);
