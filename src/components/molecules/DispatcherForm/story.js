import React from 'react';
import { storiesOf } from '@storybook/react';
import DispatcherForm from './DispatcherForm';

storiesOf('2. Molecules / DispatcherForm', module)
	.add('Default', () => <DispatcherForm />);
