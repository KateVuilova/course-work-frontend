import React from 'react';
import { storiesOf } from '@storybook/react';
import ObjectForm from './ObjectForm';

storiesOf('2. Molecules / ObjectForm', module)
	.add('Default', () => <ObjectForm />);
