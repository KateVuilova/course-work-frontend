import React from 'react';
import { storiesOf } from '@storybook/react';
import ContractForm from './ContractForm';

storiesOf('2. Molecules / ContractForm', module)
	.add('Default', () => <ContractForm />);
