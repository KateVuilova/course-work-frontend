import React from 'react';
import { storiesOf } from '@storybook/react';
import DriverForm from './DriverForm';

storiesOf('2. Molecules / DriverForm', module)
	.add('Default', () => <DriverForm />);
